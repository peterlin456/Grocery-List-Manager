<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.qc.seclass.glm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CreateItem"></activity>
        <activity android:name=".AddItemBySearch" />
        <activity android:name=".EditItem" />
        <activity android:name=".RenameList" />
        <activity android:name=".AdditemByType" />
        <activity android:name=".AddItem" />
        <activity android:name=".AddList" />
        <activity android:name=".Lists" />
        <activity android:name=".Viewname_class" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>